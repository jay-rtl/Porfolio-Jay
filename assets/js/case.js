(()=>{const B=new URL('../..',document.currentScript.src).href.replace(/\/$/,'');const slug=document.body.dataset.case;const p=window.PORTFOLIO_DATA.projects.find(x=>x.slug===slug);if(!p)return;const meta=document.querySelector('meta[name=description]');const shot=`<div class="browser"><div class="browser-bar"></div><img src="${B}/${p.image}" alt="${p.imageAlt||p.title+' website screenshot'}"></div>`;const buttons=p.url?`<a class="button button-primary" href="${p.url}" target="_blank" rel="noopener">View Live Website &nearr;</a>`:'';if(p.workflows){document.querySelector('main[data-case]').innerHTML=`<section class="page-hero"><div class="wrap case-hero"><div><div class="eyebrow">Case study &middot; ${p.projectType}</div><h1>${p.title}</h1><p class="lead">${p.subtitle}</p><p>${p.intro}</p><div class="facts">${p.tech.map(x=>`<span class="tag">${x}</span>`).join('')}</div><div class="actions">${buttons}<a class="button button-secondary" href="${B}/contact/">Discuss a similar project</a></div></div><div class="card case-screen ${p.imagePending?'asset-pending':''}">${shot}${p.imagePending?'<p class="asset-note">Dashboard screenshot placeholder — original image asset is still required.</p>':''}</div></div></section><section class="band"><div class="wrap grid grid-2"><article class="info-card"><div class="eyebrow">The problem</div><p>${p.challenge}</p></article><article class="info-card"><div class="eyebrow">The solution</div><p>${p.solution}</p></article></div></section><section><div class="wrap"><div class="section-head"><div><div class="eyebrow">System workflow</div><h2>One connected process.</h2></div><p>The workflow connects customer-facing actions with the operational steps required to complete the work.</p></div>${p.workflows.map(w=>`<div class="workflow-block"><h3>${w.title}</h3><div class="workflow" aria-label="${w.title}">${w.steps.map((s,i)=>`<span>${s}</span>${i<w.steps.length-1?'<i aria-hidden="true">→</i>':''}`).join('')}</div></div>`).join('')}</div></section><section class="band"><div class="wrap"><div class="section-head"><div><div class="eyebrow">Key features</div><h2>Designed around daily operations.</h2></div></div><div class="grid grid-3">${p.featureDetails.map(f=>`<article class="card info-card"><h3>${f.title}</h3><p>${f.text}</p></article>`).join('')}</div></div></section><section><div class="wrap grid grid-2"><div class="content"><div class="eyebrow">Technical implementation</div><h2>${p.technicalTitle}</h2><p>${p.technical}</p><div class="facts">${p.tech.map(x=>`<span class="tag">${x}</span>`).join('')}</div></div><div class="card info-card"><div class="eyebrow">Result</div><h3>${p.value}</h3><p>This case study describes practical system value without unverified numerical performance claims.</p></div></div></section>`}else{document.querySelector('main[data-case]').innerHTML=`<section class="page-hero"><div class="wrap case-hero"><div><div class="eyebrow">Case study &middot; ${p.category}</div><h1>${p.title}</h1><p class="lead">${p.description}</p><div class="facts">${p.tech.map(x=>`<span class="tag">${x}</span>`).join('')}</div><div class="actions">${buttons}<a class="button button-secondary" href="${B}/contact/">Have a similar project? Let&rsquo;s Talk</a></div></div><div class="card case-screen">${shot}</div></div></section><section class="band"><div class="wrap grid grid-3"><article class="info-card"><div class="eyebrow">Project overview</div><p>${p.description}</p></article><article class="info-card"><div class="eyebrow">The challenge</div><p>Present the business clearly, build trust, and give mobile and desktop visitors a straightforward path to the right information or enquiry.</p></article><article class="info-card"><div class="eyebrow">My solution</div><p>I built a responsive, business-focused experience using the technologies listed for this project, with clear content hierarchy and practical calls to action.</p></article></div></section><section><div class="wrap grid grid-2"><div class="content"><div class="eyebrow">Features</div><h2>What the project includes</h2><ul class="list">${p.features.map(x=>`<li>${x}</li>`).join('')}</ul></div><div class="card info-card"><div class="eyebrow">Business value</div><h3>${p.value}</h3><p>No numerical performance claims are made; this summary focuses on the practical value of the delivered website.</p></div></div></section>`}if(meta&&!p.workflows)meta.content=p.seoDescription||`${p.title}: a web project by Jay Rotol. See the project overview, solution, features, and business value.`})();
